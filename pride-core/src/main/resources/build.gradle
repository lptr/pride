// Patch tasks through to projects in included builds
tasks.addRule("Execute task from included builds") { String taskName ->
	task(taskName) {
		dependsOn gradle.includedBuilds*.configuredBuild*.rootProject*.allprojects*.tasks[taskName]
	}
}
